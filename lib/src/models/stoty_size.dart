class StorySizeModel {
  int id;
  double heigth;
  double width;
  double dy;
  double dx;
  bool isOpen;

  StorySizeModel({
    required this.id,
    required this.heigth,
    required this.width,
    required this.dy,
    required this.dx,
    required this.isOpen,
  });
}
